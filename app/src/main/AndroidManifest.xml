<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appganaderosv1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".insert_new_type"
            android:theme="@style/AppTheme.Transparent"></activity>
        <activity
            android:name=".insert_new_race"
            android:theme="@style/AppTheme.Transparent" />
        <activity android:name=".insert_new_animal" />
        <activity android:name=".insert_new_purchases" />
        <activity android:name=".appointment_details" />
        <activity android:name=".calendar" />
        <activity android:name=".insert_new_person" />
        <activity android:name=".insert_new_appointment" />
        <activity android:name=".homeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>